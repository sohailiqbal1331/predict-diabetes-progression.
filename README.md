# Diabetes Progression Predictor
A Streamlit web application that predicts the diabetes progression score using a Linear Regression model trained on the scikit-learn diabetes dataset.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
```
